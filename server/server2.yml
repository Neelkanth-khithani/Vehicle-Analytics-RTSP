rtspTransports: [tcp]
rtspAddress: :8555
rtpAddress: :8002
rtcpAddress: :8003
rtmpAddress: :1936
hlsAddress: :8081
webRTCAddress: :8891
webRTCLocalUDPAddress: :8192 # Updated parameter name
srtAddress: :8892

paths:
  camera2:
    # Removed audio encoding and bitrate options, added -an to exclude audio
    runOnInit: ffmpeg -re -stream_loop -1 -i test2.mp4 -r 15 -c:v libx264 -preset medium -crf 28 -vf scale=1280:-2 -an -f rtsp -rtsp_transport tcp rtsp://ffmpeguser1:ffmpegpass1@localhost:8555/camera2