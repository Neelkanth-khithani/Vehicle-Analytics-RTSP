rtspTransports: [tcp]
rtspAddress: :8554 # Explicitly define for clarity, though it's the default
rtpAddress: :8000
rtcpAddress: :8001
rtmpAddress: :1935
hlsAddress: :8085
webRTCAddress: :8889
webRTCLocalUDPAddress: :8190 # Updated parameter name
srtAddress: :8890

paths:
  camera1:
    # Removed audio encoding and bitrate options, added -an to exclude audio
    runOnInit: ffmpeg -re -stream_loop -1 -i test1.mp4 -r 15 -c:v libx264 -preset medium -crf 28 -vf scale=1280:-2 -an -f rtsp -rtsp_transport tcp rtsp://ffmpeguser1:ffmpegpass1@localhost:8554/camera1